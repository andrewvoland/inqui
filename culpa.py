# Define a list of keys
keys = ['a', 'b', 'c', 'd', 'e']

# Define an index
t = 2

# Retrieve the element at index t and assign it to key
key = keys[t]

# Print the result
print(key)  # Output: c
